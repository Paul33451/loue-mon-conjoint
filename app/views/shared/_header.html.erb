<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to "/", class: "navbar-brand" do %>
    <%= image_tag "LMC-logo.png" %>
  <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <% if user_signed_in? %>
    <span class="navbar-toggler">
      <% if current_user.photo.present? %>
      <%= cl_image_tag current_user.photo, class: "avatar", 'aria-haspopup': true, 'aria-expanded': false %>
      <% else %>
      <%= image_tag "https://cdn.pixabay.com/photo/2017/02/25/22/04/user-icon-2098873_1280.png", class: "avatar" , 'aria-haspopup': true, 'aria-expanded': false %>
      <% end %>
    <% else %>
      <span class="navbar-toggler-icon"></span>
    <% end %>
    </span>
  </button>


 <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to "Accueil", "/", class: "dropdown-item" %>
        </li>
        <li>
          <%= link_to "Profil", profile_path, method: :get, class: "dropdown-item"%>
        </li>
        <li>
          <%= link_to "Déconnexion", destroy_user_session_path(:authenticity_token => form_authenticity_token()), method: :delete, class: "dropdown-item"%>

        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
